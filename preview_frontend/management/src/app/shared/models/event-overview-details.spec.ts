import { EventOverviewDetails } from './event-overview-details';

describe('EventOverviewDetails', () => {
  it('should create an instance', () => {
    expect(new EventOverviewDetails()).toBeTruthy();
  });
});
